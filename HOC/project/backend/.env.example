# Configurazione database
DATABASE_URL="mysql://hoc_user:password_sicura@localhost:3306/hoc_app"

# JWT Secret (generare una stringa casuale lunga e complessa)
JWT_SECRET="your-secret-key-change-in-production"

# Ambiente (development, production, test)
NODE_ENV="development"

# Porta del server
PORT=3001

# Configurazione CORS (opzionale, separare con virgole)
# CORS_ORIGINS="http://localhost:5173,https://tuodominio.com"

# Durata token JWT (in secondi)
# JWT_EXPIRES_IN=28800 # 8 ore

# Configurazione logging (opzionale)
# LOG_LEVEL="info" # debug, info, warn, error
# LOG_FILE="logs/app.log"
